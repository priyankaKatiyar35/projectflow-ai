# Development & testing dependencies
# Install with: pip install -r requirements-dev.txt

# Production deps (re-include base)
-r requirements.txt

# Testing
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24